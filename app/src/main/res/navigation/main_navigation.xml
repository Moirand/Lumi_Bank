<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_navigation"
    app:startDestination="@id/splashFragment">

    <include app:graph="@navigation/auth_navigation" />
    <include app:graph="@navigation/dashboard_navigation" />

    <fragment
        android:id="@+id/splashFragment"
        android:name="com.example.lumibank.splashScreen.SplashFragment"
        android:label="SplashFragment"
        tools:layout="@layout/fragment_splash">

        <action
            android:id="@+id/splashFragment_to_authNavigation"
            app:destination="@id/auth_navigation"
            app:popUpTo="@id/splashFragment"
            app:popUpToInclusive="true" />
        <action
            android:id="@+id/splashFragment_to_dashboardNavigation"
            app:destination="@id/dashboard_navigation"
            app:popUpTo="@id/splashFragment"
            app:popUpToInclusive="true" />

    </fragment>

    <action
        android:id="@+id/authNavigation_to_dashboardNavigation"
        app:destination="@id/dashboard_navigation"
        app:popUpTo="@id/auth_navigation"
        app:popUpToInclusive="true" />

    <action
        android:id="@+id/dashboardNavigation_to_authNavigation"
        app:destination="@id/auth_navigation"
        app:popUpTo="@id/dashboard_navigation"
        app:popUpToInclusive="true" />

</navigation>